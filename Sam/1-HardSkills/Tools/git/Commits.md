```
git commit -m "add: ..." // do image

git branch [branch_name] // new branch
git branch -f [branch_name] HEAD~3 // all branch back 3

git checkout [branch_name] // go to branch_name
git checkout -b [branch_name] // new branch and checkout to in
git checkout HEAD^ // parent of commit
git checkout HEAD~4 == git checkout HEAD^^^^

git merge [name]
 ||
git rebase [name]

git reset

git revert

```